{
  "buildCommand": "cd src && npm install && npm run build",
  "outputDirectory": "src/dist/client",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
